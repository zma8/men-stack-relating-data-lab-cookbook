<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>My Recipes</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    
    <div class="container">
        <h1>Welcome to My Recipes</h1>
        
        <div class="actions" style="margin-bottom: 30px;">
            <a href="/recipes/new" class="btn">+ Add Recipe</a>
            <a href="/ingredients" class="btn btn-secondary">Manage Ingredients</a>
        </div>
        
        <% if (recipes && recipes.length > 0) { %>
            <div class="recipe-list">
                <% recipes.forEach((recipe) => { %>
                    <div class="card">
                        <a href="/recipes/<%= recipe._id %>" style="text-decoration: none; color: inherit;">
                            <h3><%= recipe.name %></h3>
                            <p style="color: #999999; font-size: 14px; margin-top: 10px;">
                                By <%= recipe.owner.username %>
                            </p>
                            <% if (recipe.ingredients && recipe.ingredients.length > 0) { %>
                                <p style="color: #cccccc; font-size: 14px; margin-top: 5px;">
                                    <%= recipe.ingredients.length %> ingredient<%= recipe.ingredients.length !== 1 ? 's' : '' %>
                                </p>
                            <% } %>
                        </a>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="card" style="text-align: center; padding: 40px;">
                <p style="color: #999999; margin-bottom: 20px;">No recipes found yet.</p>
                <a href="/recipes/new" class="btn">Create Your First Recipe!</a>
            </div>
        <% } %>
    </div>
</body>
</html>