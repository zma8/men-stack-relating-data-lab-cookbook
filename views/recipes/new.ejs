<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>New Recipe</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    
    <div class="container">
        <h1>Create New Recipe</h1>
        
        <form action="/recipes" method="POST">
            <label for="name">Recipe Name:</label>
            <input type="text" name="name" id="name" placeholder="e.g., Chocolate Chip Cookies" required />
            
            <label for="instructions">Instructions:</label>
            <textarea name="instructions" id="instructions" placeholder="Enter step-by-step instructions..." required></textarea>
            
            <label for="ingredients">Select Ingredients:</label>
            <p class="help-text">Hold Ctrl (or Cmd on Mac) to select multiple ingredients</p>
            <select name="ingredients" id="ingredients" multiple required>
                <% if (ingredients && ingredients.length > 0) { %>
                    <% ingredients.forEach(ingredient => { %>
                        <option value="<%= ingredient._id %>"><%= ingredient.name %></option>
                    <% }) %>
                <% } else { %>
                    <option disabled>No ingredients available</option>
                <% } %>
            </select>
            
            <% if (!ingredients || ingredients.length === 0) { %>
                <div class="alert alert-error" >
                    No ingredients found. <a href="/ingredients" >Add ingredients first</a>
                </div>
            <% } else { %>
                <p class="help-text" >
                    Need more ingredients? <a href="/ingredients" >Manage ingredients</a> (opens in new tab)
                </p>
            <% } %>
            
            <input type="hidden" name="owner" value="<%= user._id %>" />
            
            <button type="submit">Create Recipe</button>
        </form>
        
        <div class="actions">
            <a href="/recipes" class="btn btn-secondary">Cancel</a>
        </div>
    </div>
</body>
</html>